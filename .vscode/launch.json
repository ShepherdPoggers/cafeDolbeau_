{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Café Dolbeau : démarrer Django",
            "type": "debugpy",
            "request": "launch",
            "program": "${workspaceFolder}/manage.py",
            "args": ["runserver"],
            "cwd": "${workspaceFolder}",
            "django": true,
            "console": "integratedTerminal"
        }
    ]
}
